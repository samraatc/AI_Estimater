# ============================================================
# EstimateOS — AWS Production Docker Compose
# Frontend : https://valuscop.com
# API      : https://api.valuscop.com
#
# IMPORTANT: All infrastructure passwords are hardcoded here.
# Application secrets come from backend/.env.production
#
# USAGE:
#   docker compose -f docker-compose.prod.yml up -d --build
#   docker compose -f docker-compose.prod.yml logs -f api
#   docker compose -f docker-compose.prod.yml ps
# ============================================================

services:

  # ── MongoDB ───────────────────────────────────────────────────
  mongodb:
    image: mongo:7.0
    container_name: estimateos-mongo
    restart: always
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout:  5s
      retries:  5
      start_period: 15s
    networks: [estimateos-net]

  # ── Redis ─────────────────────────────────────────────────────
  redis:
    image: redis:7-alpine
    container_name: estimateos-redis
    restart: always
    command:
      - redis-server
      - --requirepass
      - "CHANGE_REDIS_PASSWORD"
      - --maxmemory
      - 512mb
      - --maxmemory-policy
      - allkeys-lru
      - --save
      - "60"
      - "1"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "CHANGE_REDIS_PASSWORD", "ping"]
      interval: 10s
      timeout:  5s
      retries:  5
      start_period: 15s
    networks: [estimateos-net]


  # ── MinIO ─────────────────────────────────────────────────────
  minio:
    image: minio/minio:latest
    container_name: estimateos-minio
    restart: always
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER:     "CHANGE_MINIO_KEY"
      MINIO_ROOT_PASSWORD: "CHANGE_MINIO_SECRET"
    volumes:
      - minio_data:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout:  10s
      retries:  3
      start_period: 20s
    networks: [estimateos-net]

  # ── MinIO Bucket Init ─────────────────────────────────────────
  minio-init:
    image: minio/mc:latest
    container_name: estimateos-minio-init
    restart: on-failure
    depends_on:
      minio:
        condition: service_healthy
    entrypoint:
      - /bin/sh
      - -c
      - |
        mc alias set local http://minio:9000 CHANGE_MINIO_KEY CHANGE_MINIO_SECRET
        mc mb --ignore-existing local/platform-system
        mc mb --ignore-existing local/quotations
        echo "MinIO buckets ready"
        exit 0
    networks: [estimateos-net]

  # ── Backend API ───────────────────────────────────────────────
  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: estimateos-api
    restart: always
    env_file:
      - ./backend/.env.production
    environment:
      # Override with Docker service hostnames
      MONGODB_URI:    "mongodb://mongodb:27017/estimateos"
      REDIS_HOST:     redis
      REDIS_PASSWORD: "CHANGE_REDIS_PASSWORD"
      MINIO_ENDPOINT: minio
      MINIO_ACCESS_KEY: "CHANGE_MINIO_KEY"
      MINIO_SECRET_KEY: "CHANGE_MINIO_SECRET"
      NODE_ENV:       production
    depends_on:
      mongodb:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:3000/api/v1/health || exit 1"]
      interval: 30s
      timeout:  10s
      retries:  5
      start_period: 60s
    networks: [estimateos-net]

  # ── AI Worker ─────────────────────────────────────────────────
  ai-worker:
    build:
      context: ./backend
      dockerfile: Dockerfile.worker
    container_name: estimateos-ai-worker
    restart: always
    env_file:
      - ./backend/.env.production
    environment:
      MONGODB_URI:      "mongodb://mongodb:27017/estimateos"
      REDIS_HOST:       redis
      REDIS_PASSWORD:   "CHANGE_REDIS_PASSWORD"
      MINIO_ENDPOINT:   minio
      MINIO_ACCESS_KEY: "CHANGE_MINIO_KEY"
      MINIO_SECRET_KEY: "CHANGE_MINIO_SECRET"
      NODE_ENV:         production
      WORKER_MODE:      "true"
      WORKER_CONCURRENCY: "3"
    depends_on:
      mongodb:
        condition: service_healthy
      redis:
        condition: service_healthy
      api:
        condition: service_healthy
    networks: [estimateos-net]

  # ── Frontend ──────────────────────────────────────────────────
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_API_URL: https://api.valuscop.com/api/v1
    container_name: estimateos-frontend
    restart: always
    depends_on:
      api:
        condition: service_healthy
    networks: [estimateos-net]

  # ── Nginx ─────────────────────────────────────────────────────
  nginx:
    build:
      context: ./nginx
      dockerfile: Dockerfile
    container_name: estimateos-nginx
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /etc/letsencrypt:/etc/letsencrypt:ro
      - /var/www/certbot:/var/www/certbot:ro
    depends_on:
      api:
        condition: service_healthy
      frontend:
        condition: service_started
    networks: [estimateos-net]

volumes:
  mongo_data:
  redis_data:
  minio_data:

networks:
  estimateos-net:
    driver: bridge
